# 2.2 – Service Discovery

---

## 🎯 Objetivo

Comprender qué es el **descubrimiento de servicios (Service Discovery)**, por qué es esencial en una arquitectura distribuida, y cómo se puede implementar —de forma manual o automatizada— para permitir que los microservicios **se encuentren y comuniquen sin depender de direcciones fijas**.

---

## 🧩 Contexto

En un entorno monolítico, todos los módulos viven dentro del mismo proceso: basta con llamar a una función o clase.
En cambio, en una arquitectura de microservicios, cada componente se ejecuta de manera independiente, normalmente en **contenedores** o **máquinas diferentes**.

Esto genera una pregunta fundamental:

> 💡 ¿Cómo sabe un servicio cómo llegar a otro si su dirección IP o puerto pueden cambiar constantemente?

En entornos con orquestadores como Kubernetes, los servicios pueden **reiniciarse, escalarse o moverse** entre nodos, lo que hace que **las direcciones sean volátiles**.
Aquí es donde entra el concepto de **Service Discovery**.

---

## ⚙️ Qué es Service Discovery

El **Service Discovery** es un mecanismo que permite a los servicios:

1. **Registrarse** cuando se inician (indicando su nombre, IP y puerto).
2. **Descubrir** a otros servicios registrados cuando necesitan comunicarse.

Esto elimina la necesidad de **configurar direcciones manualmente** en el código.

---

### 🔍 Ejemplo conceptual

Sin Service Discovery:

```js
// Dirección fija — frágil
const users = await axios.get("http://localhost:3001/users");
```

Si el servicio `users` se mueve a otro host o puerto, esta línea falla.

Con Service Discovery:

```js
// Resolución dinámica
const host = await discovery.lookup("users-service");
const users = await axios.get(`http://${host}/users`);
```

El servicio `orders` ya no necesita conocer la ubicación exacta, solo el **nombre lógico del servicio**.

---

## 🧠 Enfoques de descubrimiento

Hay dos estrategias principales:

| Estrategia                | Descripción                                                                                    | Ejemplo                             |
| ------------------------- | ---------------------------------------------------------------------------------------------- | ----------------------------------- |
| **Client-side discovery** | El cliente consulta directamente el registro para encontrar la dirección del servicio destino. | Netflix Eureka, Consul              |
| **Server-side discovery** | Un *load balancer* o *gateway* se encarga de redirigir las peticiones al servicio adecuado.    | NGINX, AWS ELB, Kubernetes Services |

---

## 🔧 Ejemplo funcional simplificado (Client-side Discovery)

Imaginemos un sistema con un **registro de servicios simple** almacenado en memoria.
Cada microservicio se registra al iniciar, y otros pueden consultar este registro.

### Registro de servicios

```js
// registry.js
const servicios = new Map();

export function registrar(nombre, host, puerto) {
  servicios.set(nombre, { host, puerto });
  console.log(`🗂️ Servicio registrado: ${nombre} (${host}:${puerto})`);
}

export function resolver(nombre) {
  return servicios.get(nombre);
}
```

### Servicio que se registra

```js
// users-service
import { registrar } from "./registry.js";
registrar("users-service", "localhost", 3001);
```

### Servicio que descubre y consulta

```js
// orders-service
import axios from "axios";
import { resolver } from "./registry.js";

async function obtenerUsuarios() {
  const servicio = resolver("users-service");
  const url = `http://${servicio.host}:${servicio.puerto}/users`;
  const res = await axios.get(url);
  console.log("Usuarios disponibles:", res.data);
}
```

Este esquema demuestra el principio fundamental:
cada servicio **se anuncia** al iniciarse, y los demás pueden **descubrirlo dinámicamente**.

---

## 🧱 Service Discovery en sistemas reales

En entornos de producción, este registro no se implementa a mano, sino que se delega en **herramientas especializadas**:

| Herramienta                   | Función                                                 | Ejemplo de uso                                   |
| ----------------------------- | ------------------------------------------------------- | ------------------------------------------------ |
| **Consul**                    | Registro clave-valor y descubrimiento vía HTTP/DNS      | `consul catalog services`                        |
| **Eureka (Netflix OSS)**      | Registro dinámico con health checks integrados          | `eureka.client.serviceUrl.defaultZone`           |
| **Kubernetes DNS / Services** | Descubrimiento automático mediante nombres DNS internos | `http://users-service.default.svc.cluster.local` |

Ejemplo en Kubernetes:

```bash
# Un pod puede resolver la IP de otro servicio
curl http://orders-service.default.svc.cluster.local
```

Kubernetes actúa como sistema de **Service Discovery nativo**, gestionando IPs, balanceo y nombres de servicio automáticamente.

---

## 🧩 Integración con comunicación síncrona y asíncrona

* En **REST**, el descubrimiento garantiza que cada servicio encuentre al otro por nombre sin depender de IPs.
* En **mensajería**, el broker (Kafka, RabbitMQ, etc.) actúa como **punto central de descubrimiento**, gestionando colas y tópicos registrados.

Ejemplo conceptual de consumidor dinámico en RabbitMQ:

```js
// Se conecta a la cola publicada en el registro
const queueName = discovery.lookup("cola_pedidos");
channel.consume(queueName, (msg) => { ... });
```

---

## ⚠️ Retos y consideraciones

| Desafío                        | Descripción                                                                   |
| ------------------------------ | ----------------------------------------------------------------------------- |
| **Consistencia**               | El registro debe reflejar el estado real de los servicios (alive/dead).       |
| **Escalado dinámico**          | Cuando un servicio tiene varias instancias, el registro debe actualizarlas.   |
| **Latencia de descubrimiento** | La búsqueda de direcciones no debe añadir retraso significativo.              |
| **Seguridad**                  | No todos los servicios deben descubrir todos los demás (autenticación, ACLs). |

---

## 🧠 Buenas prácticas

1. **Centralizar el registro** en un servicio confiable (Eureka, Consul, Kubernetes).
2. **Agregar health checks** periódicos para limpiar instancias inactivas.
3. **Evitar configuraciones estáticas** en código: usar nombres de servicio, no IPs.
4. **Integrar caché local** para no consultar el registro en cada llamada.
5. **Monitorear el registro**: un Service Discovery caído puede afectar todo el sistema.

---

## 💬 Resumen

* El **Service Discovery** permite que los microservicios **se localicen automáticamente** sin direcciones fijas.
* Existen dos modelos principales: *client-side* y *server-side*.
* Herramientas como **Consul, Eureka o Kubernetes** simplifican este proceso.
* Su correcta implementación es clave para el **despliegue dinámico y la resiliencia** en entornos distribuidos.

---

## 🔚 Conclusión

El descubrimiento de servicios es el “GPS” de una arquitectura distribuida:
sin él, los microservicios no podrían encontrarse en un entorno cambiante.
En el siguiente apartado, exploraremos cómo asegurar que esta comunicación sea **tolerante a fallos y eficiente**, mediante **balanceo de carga y patrones de resiliencia**.

---

## Navegación

[← 2.1 Comunicación entre servicios](2.1_comunicacion_entre_servicios.md) · [Temario](../../../README.md) · [Siguiente: 2.3 Balanceo y resiliencia →](2.3_balanceo_y_resiliencia.md)